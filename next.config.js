/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['image.tmdb.org','rb.gy','cdn.mos.cms.futurecdn.net','www.notebookcheck.net'],
  },
}

module.exports = nextConfig
